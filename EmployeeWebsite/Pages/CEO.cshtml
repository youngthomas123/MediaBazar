@page
@model EmployeeWebsite.Pages.CEOModel
@{
	ViewData["Title"] = "Stats";
}

<div class="progress">
    <div class="progress-bar bg-success" role="progressbar" style="width: @Model.AttendancePercentage%" aria-valuenow="@Model.AttendancePercentage" aria-valuemin="0" aria-valuemax="100"></div>
</div>
@*<div>
    <canvas id="chart"></canvas>
</div>

@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.4/Chart.min.js"></script>
    <script>
        var chartData = @Html.Raw(Model.ChartData);

        var chartConfig = {
            type: 'bar',
            data: {
                labels: chartData.map(x => x.Column1),
                datasets: [{
                    label: 'Column 2',
                    data: chartData.map(x => x.Column2),
                    backgroundColor: 'rgba(255, 99, 132, 0.2)',
                    borderColor: 'rgba(255, 99, 132, 1)',
                    borderWidth: 1
                }]
            },
            options: {
                responsive: true,
                scales: {
                    yAxes: [{
                        ticks: {
                            beginAtZero: true
                        }
                    }]
                }
            }
        };

        var chart = new Chart(document.getElementById('chart'), chartConfig);
    </script>
}*@
